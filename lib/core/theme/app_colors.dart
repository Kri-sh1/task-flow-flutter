import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceAlt = Color(0xFFF3F4F6);

  // Borders
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFD1D5DB);

  // Text
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textMuted = Color(0xFF9CA3AF);

  // Main Brand
  static const Color primary = Color(0xFF7C3AED); // Purple
  static const Color primaryHover = Color(0xFF6D28D9);
  static const Color primaryLight = Color(0xFFEDE9FE);

  // Gamification
  static const Color gold = Color(0xFFFACC15); // XP, rewards
  static const Color goldLight = Color(0xFFFEF9C3);

  // Status
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF06B6D4);

  // Priority
  static const Color lowPriority = Color(0xFF94A3B8);
  static const Color mediumPriority = Color(0xFFF59E0B);
  static const Color highPriority = Color(0xFFEF4444);
  static const Color urgentPriority = Color(0xFF7C3AED);
}